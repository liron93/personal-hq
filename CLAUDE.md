# personal-hq — הנחיות לעבודה על הפרויקט

אפליקציית Next.js (App Router) בעברית, RTL. "ניהול החיים כחברת אחזקות": מסך מנכ״ל
אחד, ומתחתיו תת-חברה לכל תחום (בית, כלכלה, אימון וכו').

## מבנה הפרויקט

```
app/
  page.jsx                   מסך המנכ״ל
  login/page.jsx             התחברות (קישור למייל)
  AuthGate.jsx                עוטף את כל האתר; בלי משתמש מחובר מפנה ל-/login
  companies/<slug>/page.jsx  עמוד לכל תת-חברה (מעטפת CompanyShell + Company מ-companies/)
companies/
  registry.js                רשימת כל תתי-החברות — המקום היחיד שמעדכנים כשמוסיפים חברה
  <slug>/                    הלוגיקה של תת-חברה אחת: model.js, Company.jsx, ורכיבי המסכים שלה
lib/
  store.js                   שכבת האחסון היחידה (ראו למטה)
  supabase.js                לקוח Supabase
  coreFacts.js                נתוני ליבה משותפים (ראו למטה)
  theme.js, ui.jsx, format.js  עיצוב ורכיבי ממשק משותפים
supabase/schema.sql          סכימת בסיס הנתונים (מריצים ידנית ב-SQL Editor)
```

## כלל 1: תת-חברה היא תיקייה, לא ענף

כל תתי-החברות חיות יחד בענף `main`, כל אחת בתיקייה משלה תחת `companies/<slug>/`.
**אין ליצור ענף גיט לתת-חברה עצמה.** ענפים משמשים למשימות בודדות בלבד (ראו README —
`beit/payments`, `kesef/setup` וכד'), לא כדרך לבודד תת-חברה מהאחרות.

להוספת תת-חברה: תיקייה חדשה ב-`companies/<slug>/` עם `model.js` (`STORE_KEY`, `INIT`,
`summarize`) ו-`Company.jsx`, עמוד עוטף ב-`app/companies/<slug>/page.jsx`, ושורה ב-
`companies/registry.js`.

## כלל 2: המנכ"ל קורא רק את summarize()

מסך המנכ״ל (`app/page.jsx`) אף פעם לא נכנס לפרטי תת-חברה. הוא קורא רק את הפונקציה
`summarize(data)` שכל תת-חברה מייצאת מ-`model.js` שלה (משימות פתוחות, התשלום הדחוף
הבא, עדכון אחרון, דגל צבע). אם מוסיפים שדה חדש שהמנכ״ל צריך לדעת עליו — מוסיפים אותו
ל-`summarize`, לא פותחים ערוץ קריאה נפרד.

## כלל 3: כל האחסון עובר דרך lib/store.js בלבד

אין לקרוא/לכתוב ל-Supabase או ל-localStorage ישירות משום קובץ אחר. כל תת-חברה (וגם
המנכ״ל) משתמשת ב-`useStore(key, initial)` מ-`lib/store.js`. זו השכבה היחידה שיודעת
איך ומאיפה בפועל נשלפים ונשמרים הנתונים (כרגע: Supabase עם נפילה חזרה ל-localStorage
כשאין רשת). שינוי עתידי באסטרטגיית האחסון = שינוי רק בקובץ הזה.

## כלל 4: נתוני ליבה משותפים מול נתוני חברה

- **נתוני ליבה (`lib/coreFacts.js`)**: מידע שיותר מתת-חברה אחת צריכה — הכנסות,
  תאריכים משמעותיים וכד'. חי במקום אחד בלבד (`useStore(coreFacts.STORE_KEY, coreFacts.INIT)`),
  ולא מועתק בתוך אף תת-חברה. תת-חברה שצריכה נתון כזה (למשל "בית חדש" צריך הכנסות
  כדי לחשב תזרים) טוענת וכותבת אותו ישירות מ-`lib/coreFacts.js` — לא שומרת עותק
  משלה ב-`model.js`.
- **נתוני חברה (`companies/<slug>/model.js`)**: מידע ששייך רק לתת-חברה אחת — פריטי
  שיפוץ, משימות חיפוש עבודה, תוכנית אימונים וכד'. נשאר בתוך ה-`model.js` של אותה
  תת-חברה בלבד.

כלל האצבע: לפני שמוסיפים שדה חדש למודל של תת-חברה, שואלים אם חברה אחרת עתידה
להזדקק לו. אם כן — הוא שייך ל-`lib/coreFacts.js`, לא למודל המקומי.

## עיצוב

עברית, RTL, לא לשנות את שפת העיצוב הקיימת (`lib/theme.js`, `lib/ui.jsx`) בלי בקשה
מפורשת. טקסט חדש — בעברית, מיושר לימין.
